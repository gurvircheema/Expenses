
<p>Its <%= Date.today.strftime("%A") %>, <%= Date.current.strftime("%B %d, %Y") %> and time is <%= Time.now.strftime("%I:%M:%S %p") %>, TimeZone: <%= Time.now.strftime("%Z") %></p>

<div class="jumbotron">

		<h1>Manage your expenses</h1>

		<% if user_signed_in? %>
		<p class="lead">

			Currently logged in as <%= link_to current_user.user, user_path(current_user), class: "" %>, <%= link_to "Log Out",destroy_user_session_path, method: "delete", class: "btn btn-lg btn-danger" %>
		</p>

		<% else %>
		<p>Already a user? <%= link_to "Log in", new_user_session_path,class: "btn btn-default btn-lg" %> New user? <%= link_to "Sign Up", new_user_registration_path,class: "btn btn-default btn-lg" %> </p>

		
		<% end %>


</div>
